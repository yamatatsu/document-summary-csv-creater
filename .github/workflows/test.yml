name: "Test document-summary-list-csv-creater"
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - run: npm ci
      - run: npm run build
      - run: npm test
      - uses: ./
        with:
          dirPath: ./sampleFiles
        id: createcsv
      - uses: finnp/create-file-action@v1.0.0
        env:
          FILE_NAME: ./sampleFiles/summary.csv
          FILE_DATA: ${{ createcsv.outputs.csv }}
      - run: cat ./sampleFiles/summary.csv
